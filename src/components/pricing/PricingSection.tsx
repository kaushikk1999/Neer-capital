import { pricingPlans } from '@/lib/data';
import { Card } from '@/components/ui/Card';
import { Button } from '@/components/ui/Button';
export function PricingSection() { return <div className="grid gap-6 lg:grid-cols-2">{pricingPlans.map((plan) => <Card key={plan.name} className={plan.featured ? 'border-blue-400/30 bg-white/[0.06]' : ''}><div className="flex items-start justify-between gap-4"><div><h3 className="text-xl font-semibold text-white">{plan.name}</h3>{plan.price && <p className="mt-2 text-sm text-blue-300">{plan.price}</p>}</div>{plan.featured && <span className="rounded-full border border-blue-400/30 bg-blue-400/10 px-3 py-1 text-xs text-blue-200">Recommended</span>}</div><p className="mt-4 text-sm leading-6 text-slate-300">{plan.description}</p><ul className="mt-5 space-y-2 text-sm text-slate-200">{plan.features.map((feature) => <li key={feature}>• {feature}</li>)}</ul><div className="mt-6"><Button href={plan.cta.href} variant={plan.featured ? 'primary' : 'secondary'}>{plan.cta.label}</Button></div></Card>)}</div>; }
